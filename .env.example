################################################################################
# EAGLE EYE ENVIRONMENT CONFIGURATION
# 
# Copy this file to .env.local and fill in your actual values
# IMPORTANT: .env.local is git-ignored and should NEVER be committed
# 
# For local development:
#   cp .env.example .env.local
#   # Edit .env.local with your local values
#
# For production, use Azure Key Vault or similar secret management
################################################################################

# Application Environment
ENVIRONMENT=development
DEBUG=false
LOG_LEVEL=INFO

################################################################################
# DATABASE
################################################################################
DATABASE_URL=postgresql+psycopg://eagle:eagle@localhost:5432/eagle
# Production example: postgresql+psycopg://user:pass@prod-db.azure.com:5432/eagle

################################################################################
# STORAGE (MinIO / S3)
################################################################################
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=eagle-files
# Production examples:
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY=AKIA...
# S3_SECRET_KEY=...

################################################################################
# CACHE (Redis)
################################################################################
REDIS_URL=redis://localhost:6379/0
# Production example: redis://prod-redis.redis.cache.windows.net:6380/0

################################################################################
# LANGUAGE MODELS & AI
################################################################################

# OpenAI Configuration
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-4-turbo-preview
# To find your OpenAI API key:
# 1. Go to https://platform.openai.com/api-keys
# 2. Create new secret key
# 3. Copy and paste here

# Ollama Configuration (local LLM, optional)
OLLAMA_ENABLED=false
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama2
OLLAMA_API_TOKEN=
# Ollama is optional for local LLM testing without OpenAI costs

# Hugging Face Configuration
HUGGINGFACE_ENABLED=false
HUGGINGFACE_API_KEY=hf_...
HUGGINGFACE_MODEL=gpt2
HUGGINGFACE_TASK=text-generation
# To get Hugging Face token: https://huggingface.co/settings/tokens
# Set HUGGINGFACE_MODEL to any model from: https://huggingface.co/models

################################################################################
# VISION & IMAGE PROCESSING
################################################################################
VISION_ENABLED=false
SAM_MODEL=sam_vit_h
GROUNDING_DINO_MODEL=groundingdino_swinb_cogcoor
# Set to true to enable advanced vision features (requires model downloads)

################################################################################
# JURISDICTION & CODE STANDARDS
################################################################################
DEFAULT_STATE=GA
CODE_SET=IRC2018_IECC2015_NEC2017_GA
# Supported states: GA, CA, TX, NY, FL

################################################################################
# API SERVER
################################################################################
API_HOST=0.0.0.0
API_PORT=8000
# Frontend CORS origins (add as needed for your deployment)
CORS_ORIGINS=http://localhost:3000,http://localhost:5678,http://localhost:8000

################################################################################
# n8n WORKFLOW ORCHESTRATION
################################################################################
N8N_ENABLED=true
N8N_WEBHOOK_URL=http://localhost:5678/webhook
N8N_API_URL=http://localhost:5678/api
N8N_API_KEY=
# Get n8n API key from: n8n UI → Settings → API Keys

################################################################################
# LOGGING & MONITORING
################################################################################
SEQ_ENABLED=false
SEQ_URL=http://localhost:5341
# Seq is optional structured logging (great for debugging)
# Sign up at https://datalust.co/seq

################################################################################
# SECURITY NOTES
################################################################################
# 
# 1. NEVER commit .env.local to git
#    → It's in .gitignore but double-check!
#
# 2. Rotate secrets regularly
#    → If exposed, create new API keys immediately
#
# 3. Use strong database passwords in production
#    → Min 20 characters, mix of case/symbols/numbers
#
# 4. For production deployments:
#    → Use Azure Key Vault (C# services)
#    → Use GitHub Secrets (Actions)
#    → Use environment variables (Docker, Kubernetes)
#
# 5. To check for exposed secrets:
#    → Run: git log -S "sk-" --all  (find commits with OpenAI keys)
#    → Use: git-secrets or truffleHog
#
################################################################################
API_HOST=0.0.0.0
API_PORT=8000

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
